name: byte_bite
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.4

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  printing: ^5.13.3
  pdf: ^3.11.1
  image_picker: ^1.0.7
  sqflite: ^2.3.0
  path: ^1.8.3
  firebase_core: ^3.0.0
  firebase_auth: ^5.0.0
  cloud_firestore: ^5.0.0
  connectivity_plus: ^7.0.0
  crypto: ^3.0.3
  http: ^1.2.2
  path_provider: ^2.1.4
  # ── Offline + Cloud Sync ──────────────────────────────────────────────────
  # Used by homepage.dart to detect network status and show sync indicator.

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/